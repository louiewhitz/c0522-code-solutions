/* exported firstChars */
function firstChars(length, string) {
  return string.slice(0, length);
}

// i should get the first character of the string
// first character is at index 0, staring is a secret array
// start the splice at index 0 and let the end index just be the length
