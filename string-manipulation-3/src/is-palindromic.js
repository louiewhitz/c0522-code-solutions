// /* exported isPalindromic */
// function isPalindromic(string) {
//   var allStr = string.replace(' ', '');
//   var halfString = allStr.length;
//   for (var i = 0; i < allStr.length / 2; i++) {
//     if (allStr[i] !== allStr[halfString - 1 - i]) {
//       return false;
//     }
//   }
//   return true;
// }
//   var half = string.length;
//   for (var i = 0; i < string.length / 2; i++); {
//     var half = string[i];
//   if (string[i] !== half[len - 1 - i]) {
//     return false;
//   } else if
// Palindrome means that something reads backward and forward the same
// get the length of what the word is
// go through the word and and single out the ccharacters
// divide the word in half
// check if the first and last character are the same
