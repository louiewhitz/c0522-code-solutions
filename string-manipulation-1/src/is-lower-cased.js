/* exported isLowerCased */
function isLowerCased(word) {
  var lowerCase = word.toLowerCase();
  if (word === lowerCase) {
    return true;
  } else {
    return false;
  }
}

// Here we are trying to see if the entire word is lowercase
// create a variable that stores the parameter and set it to lowercase
// create an if statement that asks the question whether the word is in fact lowercase
// if it is, return true
// if it isnt, return false
